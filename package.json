{
  "name": "goto",
  "version": "0.0.1",
  "private": true,
  "description": "Workspace root for the goto monorepo (price, dispatch, frontend).",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "proto:sync": "for d in services/*/src/proto; do cp proto/*.proto \"$d/\"; done",
    "proto:all": "pnpm proto:sync && (cd proto && buf generate) && pnpm -r run proto:generate"
  },
  "packageManager": "pnpm@11.3.0"
}
