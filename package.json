{
  "name": "goto",
  "version": "0.0.1",
  "private": true,
  "description": "Workspace root for the goto monorepo (price, dispatch, frontend).",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "dev:cleardb": "docker compose exec -T postgres psql -U postgres -d goto -v ON_ERROR_STOP=1 -c 'TRUNCATE TABLE public.offered, public.rides, public.drivers RESTART IDENTITY CASCADE;' && docker compose exec -T redis redis-cli FLUSHALL",
    "proto:all": "pnpm --filter @goto/proto run generate"
  },
  "packageManager": "pnpm@11.3.0"
}
